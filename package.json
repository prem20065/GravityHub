{
  "name": "gravityhub",
  "version": "1.0.0",
  "description": "A productivity and wellness platform",
  "scripts": {
    "dev": "concurrently \"cd frontend && npm run dev\" \"cd backend && python app.py\"",
    "build": "cd frontend && npm run build",
    "start": "cd backend && python app.py",
    "install-all": "npm install && cd frontend && npm install && cd .."
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
